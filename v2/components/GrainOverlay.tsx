"use client";
// Fixed full-screen animated grain/noise overlay.
// Uses CSS animation to shift the SVG turbulence texture every 2 frames.
export function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden="true" />;
}
